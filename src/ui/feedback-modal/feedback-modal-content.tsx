import { ReactNode } from 'react';

interface FeedbackModalContentProps {
    children: ReactNode;
}

export function FeedbackModalContent({ children }: FeedbackModalContentProps) {
    return children;
}
